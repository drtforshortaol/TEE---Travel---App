window.__TEE_PARTS__=window.__TEE_PARTS__||{};window.__TEE_PARTS__["traveler-source-flow-v3415"]=window.__TEE_PARTS__["traveler-source-flow-v3415"]||[];window.__TEE_PARTS__["traveler-source-flow-v3415"].push("on vaultOpen(){ return vaultBridge()?.getState?.() === 'unlocked'; }\n  function hubSessionProfile(){ return window.TEEVaultSession?.get?.()?.profileId || null; }\n  function currentPrivateTarget(){ return activeProfile()==='coupleB'?'coupleB':hubSessionProfile()==='coupleB'?'coupleB':'coupleA'; }\n  function selectedAccess(){ return ui.target.value==='shared'?'shared':ui.target.value?'private':''; }\n  function selectedTargetProfile(){ return ui.target.value==='shared'||!ui.target.value?null:ui.target.value; }\n  function selectedTargetLabel(){\n    if(ui.target.value==='shared') return 'Shared — both couples';\n    if(ui.target.value==='coupleB')return 'Private — Couple B only';\n    if(ui.target.value==='coupleA')return 'Private — Couple A only';\n    return 'Choose Privacy / destination';\n  }\n  function suggestedTarget(){\n    if(['Flight','Rail','Hotel'].includes(ui.type.value)) return 'shared';\n    return '';\n  }\n  function profileLabel(profile){ return profile==='coupleB'?'Couple B':profile==='coupleA'?'Couple A':'None'; }\n  function authorizationNeeded(){\n    const access=selectedAccess(),target=selectedTargetProfile();\n    if(!access)return true;\n    if(access==='private') return !vaultOpen() || activeProfile()!==target;\n    return !vaultOpen();\n  }\n  function currentAuthorizationText(){\n    if(vaultOpen())return profileLabel(activeProfile());\n    const session=hubSessionProfile();\n    return session?`${profileLabel(session)} Hub session (Vault must be authorized here to save)`:'None';\n  }\n  function authorizationStateHtml(){\n    return `<div class=\"tee-v3404-auth-state\"><div><span>Current authorization</span><strong>${esc(currentAuthorizationText())}</strong></div><div><span>Destination</span><strong>${esc(selectedTargetLabel())}</strong></div></div>`;\n  }\n  function updateAuthorizationBox(){\n    if(!ui.authorization)return;\n    const needed=authorizationNeeded();\n    ui.authorization.hidden=!needed;\n    ui.save.disabled=needed || !duplicateAcknowledged;\n    if(!needed){\n      ui.authorizationPassphrase.value='';\n      return;\n    }\n    if(selectedAccess()==='private') {\n      const label=profileLabel(selectedTargetProfile());\n      ui.authorizationTitle.textContent=`${label} authorization required before save`;\n      ui.authorizationText.innerHTML=`${authorizationStateHtml()}<p>TEE will authorize <strong>${esc(label)}</strong> on this screen before saving. Enter the ${esc(label)} passphrase, then tap <strong>Authorize Selected Couple</strong>.</p>`;\n      ui.authorizationLabel.textContent=`${label} passphrase`;\n      if(ui.authorize)ui.authorize.textContent=`Authorize ${label}`;\n    } else {\n      ui.authorizationTitle.textContent='Vault authorization required before save';\n      ui.authorizationText.innerHTML=`${authorizationStateHtml()}<p>This Shared document can be saved after either authorized couple opens the Vault on this screen.</p>`;\n      ui.authorizationLabel.textContent='Either authorized couple passphrase';\n      if(ui.authorize)ui.authorize.textContent='Authorize Vault';\n    }\n  }\n  async function authorizeSelectedVault(){\n    if(!authorizationNeeded()){\n      updateAuthorizationBox();\n      setStatus('green','Correct Vault is authorized',`${currentAuthorizationText()} is active and matches ${selectedTargetLabel()}.`,'Review the source and saved details, then tap Save to TEE Vault.');\n      return true;\n    }\n    const bridge=vaultBridge();\n    if(!bridge?.authorizeWithPassphrase){\n      setStatus('red','Authorization engine is not ready','TEE cannot authorize the destination Vault on this screen.','Return to Hub, tap Refresh / Update, then try again.');\n      return false;\n    }\n    const secret=ui.authorizationPassphrase?.value||'';\n    if(!secret){\n      const requested=selectedAccess()==='private'?`${profileLabel(selectedTargetProfile())} passphrase`:'an authorized couple passphrase';\n      setStatus('yellow','Authorization required',`${selectedTargetLabel()} cannot be saved until the correct Vault is authorized.` ,`Enter ${requested}, then tap ${ui.authorize?.textContent||'Authorize Selected Couple'}.`);\n      ui.authorizationPassphrase?.focus();\n      return false;\n    }\n    const expected=selectedAccess()==='private'?selectedTargetProfile():null;\n    setStatus('yellow','Authorizing destination Vault',`TEE is checking the passphrase for ${selectedTargetLabel()}. The passphrase will be cleared immediately after this check.`,'Wait for authorization to finish.');\n    if(ui.authorize)ui.authorize.disabled=true;\n    try{\n      const result=await bridge.authorizeWithPassphrase(expected,secret);\n      ui.authorizationPassphrase.value='';\n      updateAuthorizationBox();\n      ui.save.disabled=!duplicateAcknowledged;\n      if(!duplicateAcknowledged){\n        setStatus('yellow','Destination Vault authorized — duplicate decision still required',`Current Vault: ${result?.profileLabel||profileLabel(expected)}. Destination: ${selectedTargetLabel()}. TEE found another active record for this traveler and document type.`,'Review the Possible Duplicate notice and tap Continue as New Document only if this is intentionally separate.');\n      }else if(ui.type.value==='Passport' && lastOcrResult && (lastOcrResult.found||0)<5){\n        setStatus('yellow',`${result?.profileLabel||profileLabel(expected)} authorized — passport details still need review`,`Current Vault: ${result?.profileLabel||profileLabel(expected)}. Destination: ${selectedTargetLabel()}. Local MRZ scan proposed ${lastOcrResult.found||0} of 5 required passport fields.`,'Compare the proposed values, enter any missing value you can verify, then tap Save to TEE Vault. You may also save incomplete and finish it later under Needs Attention.');\n      }else{\n        setStatus('green',`${result?.profileLabel||profileLabel(expected)} authorized`,`Current Vault: ${result?.profileLabel||profileLabel(expected)}. Destination: ${selectedTargetLabel()}.`,'Review every image/page and the key details, then tap Save to TEE Vault.');\n      }\n      return true;\n    }catch(err){\n      ui.authorizationPassphrase.value='';\n      updateAuthorizationBox();\n      setStatus('red','Vault authorization failed',err?.message||String(err),`Enter the passphrase for ${selectedTargetLabel()} and tap ${ui.authorize?.textContent||'Authorize Selected Couple'} again.`);\n      ui.authorizationPassphrase?.focus();\n      return false;\n    }finally{if(ui.authorize)ui.authorize.disabled=false;}\n  }\n  function refreshReadyStatus(){\n    const owner=ui.owner.value.trim(),target=ui.target.value;\n    const ready=!!owner&&!!preparedSource&&!!target;\n    ui.continue.disabled=!ready;\n    if(!owner&&!preparedSource){setStatus('yellow','Start with the document','Enter the Traveler / owner, choose Privacy / destination, and choose the source document(s).','Complete all three items below.');return;}\n    if(!owner){setStatus('yellow','Traveler / owner is missing','The document cannot continue until TEE knows whose document it is.','Enter the Traveler / owner.');return;}\n    if(!target){setStatus('yellow','Privacy / destination is missing','TEE will not guess which couple should receive a private document.','Choose Privacy / destination.');return;}\n    if(!preparedSource){setStatus('yellow','Source document is missing','TEE knows the traveler and destination, but no image or PDF is attached yet.','Tap Choose Source Document(s).');return;}\n    if(authorizationNeeded()){\n      setStatus('yellow','Ready to review — destination authorization still required',`${owner}'s ${ui.type.value} has ${sourceDescription()} attached. Current Vault: ${currentAuthorizationText()}. Destination: ${selectedTargetLabel()}.`,'Tap Continue to Review. TEE will require destination authorization before Save.');\n      return;\n    }\n    setStatus('green','Ready to review',`${owner}'s ${ui.type.value} has ${sourceDescription()} attached. Current Vault matches ${selectedTargetLabel()}.`,'Tap Continue to Review.');\n  }\n\n  async function compressImage(file,targetBytes=MAX_SOURCE_BYTES){\n    const url=URL.createObjectURL(file);\n    try{\n      const img=await new Promise((resolve,reject)=>{const i=new Image();i.onload=()=>resolve(i);i.onerror=()=>reject(new Error('Unable to read this image.'));i.src=url;});\n      const attempts=[\n        {max:2200,q:.90},{max:1900,q:.84},{max:1600,q:.78},{max:1350,q:.73},{max:1150,q:.68},{max:950,q:.64}\n      ];\n      for(const a of attempts){\n        const scale=Math.min(1,a.max/Math.max(img.naturalWidth,img.naturalHeight));\n        const canvas=document.createElement('canvas');\n        canvas.width=Math.max(1,Math.round(img.naturalWidth*scale)); canvas.height=Math.max(1,Math.round(img.naturalHeight*scale));\n        canvas.getContext('2d').drawImage(img,0,0,canvas.width,canvas.height);\n        const blob=await new Promise(resolve=>canvas.toBlob(resolve,'image/jpeg',a.q));\n        if(blob&&blob.size<=targetBytes)return blob;\n      }\n      throw new Error('An image is still too large after TEE prepared a smaller copy. Crop the photo to the document and try again.');\n    } finally {URL.revokeObjectURL(url);}\n  }\n\n  async function prepareSource(file){\n    const allowed=new Set(['application/pdf','image/jpeg','image/png','image/webp']);\n    if(!allowed.has(file.type))throw new Error('Choose a PDF, JPEG, PNG, or WebP document.');\n    let working=file, name=file.name, type=file.type;\n    if(file.size>MAX_SOURCE_BYTES){\n      if(!file.type.startsWith('image/'))throw new Error('This PDF is too large for the local TEE Vault. Save or scan a smaller PDF and try again.');\n      working=await compressImage(file,MAX_SOURCE_BYTES); name=file.name.replace(/\\.[^.]+$/,'')+'-TEE.jpg'; type='image/jpeg';\n    }\n    return {name,type,bytes:working.size,dataUrl:await fileToDataUrl(working),addedAt:new Date().toISOString()};\n  }\n\n  async function prepareImageForBundle(file,targetBytes,index,count){\n    if(!file.type.startsWith('image/'))throw new Error('For multiple-source selection, choose image files only. Select a PDF by itself.');\n    let working=file, name=file.name, type=file.type;\n    if(file.size>targetBytes || !['image/jpeg'].includes(file.type)){\n      working=await compressImage(file,targetBytes);\n      name=file.name.replace(/\\.[^.]+$/,'')+'-TEE.jpg';\n      type='image/jpeg';\n    }\n    return {\n      name,type,bytes:working.size,dataUrl:await fileToDataUrl(working),addedAt:new Date().toISOString(),\n      label:count===2?(index===0?'Front / Image 1':'Back / Image 2'):`Image ${index+1}`\n    };\n  }\n\n  async function prepareSourceSelection(fileList){\n    const files=Array.from(fileList||[]);\n    if(!files.length)return null;\n    if(files.length===1)return prepareSource(files[0]);\n    if(files.length>MAX_MULTI_IMAGE_COUNT)throw new Error(`Choose up to ${MAX_MULTI_IMAGE_COUNT} images for one TEE document.`);\n    if(files.some(file=>file.type==='application/pdf'))throw new Error('Select one PDF by itself, or select multiple images together. TEE keeps a multi-page PDF as one document.');\n    if(files.some(file=>!file.type.startsWith('image/')))throw new Error('Multiple-source documents must be images. Select a PDF by itself.');\n    const targetBytes=Math.min(MAX_MULTI_IMAGE_BYTES,Math.floor(MAX_MULTI_TOTAL_BYTES/files.length));\n    const prepared=[];\n    for(let i=0;i<files.length;i++)prepared.push(await prepareImageForBundle(files[i],targetBytes,i,files.length));\n    const total=prepared.reduce((sum,file)=>sum+(file.bytes||0),0);\n    if(total>MAX_MULTI_TOTAL_BYTES)throw new Error('The combined images are too large for safe local Vault storage. Crop the images more tightly and try again.');\n    return {\n      kind:'tee-source-bundle-v1',\n      name:`${prepared.length} source images`,\n      type:'application/x-tee-source-bundle',\n      bytes:total,\n      files:prepared,\n      addedAt:new Date().toISOString()\n    };\n  }\n\n  function sourceCount(){\n    return preparedSource?.kind==='tee-source-bundle-v1'?preparedSource.files.length:(preparedSource?1:0);\n  }\n  function sourceDescription(){\n ");
